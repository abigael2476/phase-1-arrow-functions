// Function expression
const divide = function () {
  return 2000 / 100;
};

// Arrow function with one parameter
const square = (x) => x * x;

// Arrow function with two parameters
const add = (a, b) => a + b;
